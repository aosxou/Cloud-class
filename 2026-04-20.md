# 클라우드 시스템 구축 중간고사 모의고사

**학과**: 컴퓨터소프트웨어공학과  
**학년**: 3학년  
**시험 시간**: 90분  
**총점**: 100점

---

## 📋 시험 안내사항

1. 문제를 잘 읽고 답안지에 답을 작성하시오.
2. 단답형은 간결하게, 서술형은 명확하게 작성하시오.
3. 명령어 문제는 정확한 문법으로 작성하시오.
4. 부분 점수가 있으니 아는 만큼 작성하시오.

---

## Part 1: 객관식 (30점, 각 2점)

### 1. 다음 중 클라우드 컴퓨팅의 특징이 **아닌** 것은?
```
① 온디맨드 방식으로 자원 제공
② 초기 투자 비용이 큼
③ 탄력적인 확장 및 축소 가능
④ 사용한 만큼 비용 지불
```

### 2. AWS 서비스 유형과 예시가 **잘못** 연결된 것은?
```
① IaaS - EC2, S3
② PaaS - Lambda, RDS
③ SaaS - WorkDocs, Chime
④ IaaS - Google Workspace
```

### 3. AWS 리전(Region)에 대한 설명으로 **올바른** 것은?
```
① 하나의 리전은 1개의 가용영역으로 구성된다
② 모든 AWS 서비스는 모든 리전에서 동시에 출시된다
③ 서울 리전의 코드는 ap-northeast-2이다
④ 리전은 논리적 개념이며 물리적 위치는 없다
```

### 4. 다음 중 **글로벌 서비스**는?
```
① EC2
② S3
③ IAM
④ VPC
```

### 5. 가용 영역(AZ)에 대한 설명으로 **틀린** 것은?
```
① 하나 이상의 물리 데이터센터를 묶은 논리적 단위이다
② AZ 간에는 매우 빠른 전용 네트워크로 연결된다
③ 모든 AZ는 서로 100km 이내에 위치한다
④ 모든 계정에서 AZ 코드와 실제 위치는 동일하다
```

### 6. EC2 인스턴스 상태 중 "중지(stopped)"에 대한 설명으로 **올바른** 것은?
```
① 인스턴스가 완전히 삭제되어 복구 불가능하다
② EC2 과금은 중단되지만 EBS 과금은 유지된다
③ 재시작이 불가능하다
④ 모든 데이터가 삭제된다
```

### 7. 인스턴스 프로파일(Instance Profile)의 역할은?
```
① EC2의 하드웨어 사양을 정의
② EC2에 IAM 역할을 부여하는 컨테이너
③ EC2의 네트워크 설정을 관리
④ EC2의 백업 정책을 설정
```

### 8. S3에 대한 설명으로 **틀린** 것은?
```
① 객체 스토리지 서비스이다
② 애플리케이션을 설치할 수 있다
③ 단일 객체는 최대 5TB까지 저장 가능하다
④ Standard 기준 최소 3개 AZ에 데이터를 복제한다
```

### 9. S3의 내구성(Durability)과 가용성(Availability)으로 **올바른** 것은?
```
① 내구성 99.99%, 가용성 99.999999999%
② 내구성 99.999999999%, 가용성 99.99%
③ 내구성 99.9%, 가용성 99.999%
④ 내구성 99.99%, 가용성 99.9%
```

### 10. IAM 정책에서 Allow와 Deny가 함께 존재할 때의 동작은?
```
① Allow가 우선 적용된다
② Deny가 우선 적용된다
③ 먼저 작성된 것이 우선 적용된다
④ 둘 다 무시된다
```

### 11. Git의 3단계 구조를 순서대로 나열한 것은?
```
① Working Directory → Repository → Staging Area
② Working Directory → Staging Area → Repository
③ Staging Area → Working Directory → Repository
④ Repository → Staging Area → Working Directory
```

### 12. 새로 생성한 파일의 Git 상태는?
```
① Unmodified
② Modified
③ Staged
④ Untracked
```

### 13. `git reset --hard [commit ID]`의 동작은?
```
① 커밋만 취소하고 파일은 Staged 상태로 유지
② 커밋과 add를 취소하고 파일은 Modified 상태로 유지
③ 커밋, add, 파일 변경을 모두 취소
④ 아무 동작도 하지 않음
```

### 14. 협업 시 커밋을 되돌릴 때 권장되는 명령어는?
```
① git reset
② git revert
③ git delete
④ git undo
```

### 15. `git push -u origin main`에서 `-u` 옵션의 역할은?
```
① 강제로 push한다
② upstream을 설정한다
③ 파일을 업데이트한다
④ 사용자 정보를 입력한다
```

---

## Part 2: 단답형 (20점, 각 2점)

### 16. 온프레미스(On-Premise)의 정의를 한 문장으로 쓰시오. (2점)

```
답:




```

---

### 17. CloudFront 서비스를 가장 빠르게 제공하기 위한 AWS의 거점을 무엇이라고 하는가? (2점)

```
답:




```

---

### 18. EC2 인스턴스에 SSH 접속하기 위해 필요한 인증 수단을 무엇이라고 하는가? (2점)

```
답:




```

---

### 19. S3에서 객체의 이름이자 경로 역할을 하는 속성의 이름은? (2점)

```
답:




```

---

### 20. Git에서 추적하지 않을 파일/폴더를 지정하는 파일의 이름은? (2점)

```
답:




```

---

### 21. `git config --global user.name "홍길동"` 명령어에서 `--global`의 의미는? (2점)

```
답:




```

---

### 22. merge 충돌이 발생했을 때 병합을 취소하는 명령어를 쓰시오. (2점)

```
답:




```

---

### 23. rebase 충돌을 해결한 후 rebase를 계속 진행하는 명령어를 쓰시오. (2점)

```
답:




```

---

### 24. 원격 저장소 목록을 확인하는 Git 명령어를 쓰시오. (2점)

```
답:




```

---

### 25. VPC의 정식 명칭(Full name)을 쓰시오. (2점)

```
답:




```

---

## Part 3: AWS CLI 명령어 작성 (15점, 각 3점)

### 26. `my-bucket` 버킷의 파일 목록을 확인하는 AWS CLI 명령어를 작성하시오. (3점)

```bash
답:




```

---

### 27. 로컬의 `test.txt` 파일을 `my-bucket`의 `files` 폴더에 업로드하는 명령어를 작성하시오. (3점)

```bash
답:




```

---

### 28. `my-bucket`의 `images` 폴더 전체를 로컬의 `downloads` 폴더로 다운로드하는 명령어를 작성하시오. (3점)

```bash
답:




```

---

### 29. `my-bucket`의 `index.php` 파일을 `/var/www/html/` 경로로 다운로드하되, 서울 리전(ap-northeast-2)을 지정하는 명령어를 작성하시오. (3점)

```bash
답:




```

---

### 30. `downloads` 폴더 전체를 `my-bucket`의 `backup` 폴더로 업로드하는 명령어를 작성하시오. (3점)

```bash
답:




```

---

## Part 4: IAM 정책 작성 (20점)

### 31. 다음 요구사항을 만족하는 S3 버킷 정책을 JSON 형식으로 작성하시오. (10점)

**요구사항:**
- 버킷 이름: `company-website`
- 모든 사람이 `public` 폴더의 파일만 다운로드(GetObject) 가능
- Effect: Allow 사용

```json
답:


















```

---

### 32. 다음 요구사항을 만족하는 S3 버킷 정책을 JSON 형식으로 작성하시오. (10점)

**요구사항:**
- 버킷 이름: `secure-data`
- IP 주소 `203.0.113.5`에서만 접근 가능
- `index.html` 파일과 `assets/images` 폴더의 모든 파일 다운로드 가능
- Condition으로 IP 제어

```json
답:


























```

---

## Part 5: Git 명령어 작성 (15점, 각 3점)

### 33. 새로운 브랜치 `feature`를 생성하면서 동시에 해당 브랜치로 이동하는 명령어를 작성하시오. (3점)

```bash
답:




```

---

### 34. `feature` 브랜치를 `main` 브랜치에 병합(merge)하는 명령어를 순서대로 작성하시오. (3점)

```bash
답:




```

---

### 35. 커밋과 add를 모두 취소하되, 파일은 수정된(Modified) 상태로 유지하는 reset 명령어를 작성하시오. (commit ID는 abc123이라고 가정) (3점)

```bash
답:




```

---

### 36. GitHub 저장소 주소를 `origin`이라는 이름으로 원격 저장소에 등록하는 명령어를 작성하시오. (저장소 주소: https://github.com/username/repo.git) (3점)

```bash
답:




```

---

### 37. Git 계정 정보(user.name과 user.email)를 초기화하는 명령어를 작성하시오. (3점)

```bash
답:




```

---

## Part 6: 서술형 (20점)

### 38. S3 정적 웹 호스팅 설정 절차를 5단계로 나누어 순서대로 서술하시오. (10점)

```
답:

1단계:


2단계:


3단계:


4단계:


5단계:


```

---

### 39. EC2 인스턴스의 "중지(stopped)"와 "종료(terminated)"의 차이를 3가지 이상 서술하시오. (10점)

```
답:

차이점 1:



차이점 2:



차이점 3:



```

---

## 🎯 수고하셨습니다!

시험지를 제출하기 전에 다시 한 번 확인하세요.

---

# 정답 및 해설

## Part 1: 객관식 (30점)

1. **②** - 클라우드는 초기 투자 비용이 적고 사용한 만큼 지불
2. **④** - Google Workspace는 SaaS
3. **③** - 서울 리전: ap-northeast-2
4. **③** - IAM은 글로벌 서비스
5. **④** - 계정별로 AZ 코드와 실제 위치는 랜덤
6. **②** - 중지 시 EC2 과금 중단, EBS 과금 유지
7. **②** - EC2에 IAM 역할을 부여하는 컨테이너
8. **②** - S3는 객체 스토리지로 애플리케이션 설치 불가
9. **②** - 내구성 11개9, 가용성 4개9
10. **②** - Deny가 Always보다 우선
11. **②** - Working Directory → Staging Area → Repository
12. **④** - Untracked
13. **③** - 모든 변경사항 완전 삭제
14. **②** - revert (히스토리 보존)
15. **②** - upstream 설정

---

## Part 2: 단답형 (20점)

16. **자체적인 공간과 자원을 이용하여 사용자가 직접 구축/운영하는 전통적 방식**

17. **엣지 로케이션(Edge Location)**

18. **키페어(Key Pair)**

19. **Key**

20. **.gitignore**

21. **이 컴퓨터의 모든 Git 프로젝트에 적용** 또는 **전역 설정**

22. **git merge --abort**

23. **git rebase --continue**

24. **git remote -v**

25. **Virtual Private Cloud**

---

## Part 3: AWS CLI (15점)

26. 
```bash
aws s3 ls s3://my-bucket/
```

27. 
```bash
aws s3 cp test.txt s3://my-bucket/files/
```

28. 
```bash
aws s3 cp s3://my-bucket/images ./downloads --recursive
```

29. 
```bash
aws s3 cp s3://my-bucket/index.php /var/www/html/ --region ap-northeast-2
```

30. 
```bash
aws s3 cp ./downloads s3://my-bucket/backup --recursive
```

---

## Part 4: IAM 정책 (20점)

31. 
```json
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Effect": "Allow",
      "Principal": "*",
      "Action": "s3:GetObject",
      "Resource": "arn:aws:s3:::company-website/public/*"
    }
  ]
}
```

**채점 기준:**
- Version: 2점
- Effect: Allow: 2점
- Principal: "*": 2점
- Action: s3:GetObject: 2점
- Resource 경로 정확: 2점

---

32. 
```json
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Effect": "Allow",
      "Principal": "*",
      "Action": "s3:GetObject",
      "Resource": [
        "arn:aws:s3:::secure-data/index.html",
        "arn:aws:s3:::secure-data/assets/images/*"
      ],
      "Condition": {
        "IpAddress": {
          "aws:SourceIp": "203.0.113.5"
        }
      }
    }
  ]
}
```

**채점 기준:**
- Version: 1점
- Effect: Allow: 1점
- Principal: "*": 1점
- Action: s3:GetObject: 2점
- Resource 배열 및 경로: 2점
- Condition 블록: 2점
- IpAddress 정확: 1점

---

## Part 5: Git 명령어 (15점)

33. 
```bash
git switch -c feature
```

34. 
```bash
git switch main
git merge feature
```
**채점 기준:** 순서 맞아야 함 (각 1.5점)

35. 
```bash
git reset --mixed abc123
```
또는
```bash
git reset abc123
```
(--mixed는 기본값)

36. 
```bash
git remote add origin https://github.com/username/repo.git
```

37. 
```bash
git config --global --unset user.name
git config --global --unset user.email
```
**채점 기준:** 두 명령어 모두 있어야 함 (각 1.5점)

---

## Part 6: 서술형 (20점)

38. **S3 정적 웹 호스팅 5단계** (각 2점)

```
1단계: S3 버킷 생성
2단계: index.html 및 정적 파일 업로드
3단계: 퍼블릭 액세스 허용 (차단 해제)
4단계: 버킷 정책 추가 (s3:GetObject 허용)
5단계: 정적 웹사이트 호스팅 활성화
```

**채점 기준:**
- 순서가 맞아야 함
- 핵심 키워드 포함 (퍼블릭 액세스, 버킷 정책, 호스팅 활성화)

---

39. **중지 vs 종료 차이** (각 3.3점, 3가지 이상)

```
차이점 1: 
- 중지: 재시작 가능
- 종료: 복구 불가능

차이점 2:
- 중지: EC2 과금 중단 (EBS 과금은 유지)
- 종료: 모든 과금 중단

차이점 3:
- 중지: EBS 데이터 유지
- 종료: 모든 데이터 삭제

차이점 4 (추가):
- 중지: 인스턴스 유형 변경 가능
- 종료: 변경 불가 (이미 삭제됨)
```

**채점 기준:**
- 3가지 이상 작성
- 각 차이점이 명확하게 대비되어야 함
- 키워드: 재시작, 과금, 데이터

---

## 📊 예상 점수 분포

- **90점 이상**: 매우 우수 (A+)
- **80~89점**: 우수 (A)
- **70~79점**: 양호 (B+)
- **60~69점**: 보통 (B)
- **60점 미만**: 재학습 필요

---

## 💡 오답 체크리스트

틀린 문제 번호를 체크하고 다시 공부하세요!

### AWS 기초
- [ ] 1, 2, 3번 (클라우드 개념, 서비스 유형, 리전)
- [ ] 4, 5번 (글로벌/리전 서비스, AZ)

### EC2 & S3
- [ ] 6, 7, 8번 (EC2 상태, 인스턴스 프로파일, S3)
- [ ] 9번 (내구성/가용성)
- [ ] 26~30번 (AWS CLI)

### IAM
- [ ] 10번 (Allow vs Deny)
- [ ] 31, 32번 (정책 작성)

### Git
- [ ] 11, 12, 13, 14, 15번 (Git 기초)
- [ ] 33~37번 (Git 명령어)
- [ ] 20~24번 (Git 단답)

### 종합
- [ ] 38번 (S3 호스팅)
- [ ] 39번 (EC2 중지/종료)